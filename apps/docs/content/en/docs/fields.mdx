---
title: Fields
description: Reusable fields in Zormy — key, schema, render, dependencies, and nesting
---

import { Callout } from "nextra/components";

import { fieldExampleCode } from "../snippets/field-example";

# Fields

Fields in Zormy are **reusable components** that encapsulate:

- **Key** — unique identifier in the form (e.g., `"name"`, `"user.email"`)
- **Schema** — Zod validation (static or dynamic)
- **Rendering** — how the field is displayed in the UI

## Creating a field: `field(key)`

Use the fluent builder `field("key")`:

<CodeExample editorHeight={360} code={fieldExampleCode} />

In `render`, you receive `register`, `fieldState`, `control`, `getValues`, `setValue`, `watch`, and other react-hook-form functions, typed for that field. Use `register()` without arguments to use the field key; to override it: `register({ name: "other.key" })`.

## Abstract field: `abstractField()`

When multiple fields share the same type and UI, create a **template** with `abstractField()` and derive fields with `.extend({ key })`:

```tsx
import { z } from "zod";
import { abstractField } from "zormy";

const BaseTextField = abstractField()
	.schema(z.string().min(3))
	.render(({ register, fieldState }) => (
		<div>
			<input {...register()} />
			{fieldState.error && <span>{fieldState.error.message}</span>}
		</div>
	));

const NameField = BaseTextField.extend({ key: "name" });
const EmailField = BaseTextField.extend({ key: "email" });
```

You can also override `schema`, `render`, `dependsOn`, or `props` in `.extend()`.

## Fields with dependencies: `.dependsOn()`

For schemas that depend on other fields, use `.dependsOn(OtherField)` and `.schema()` with a **function** that receives `formValues`:

```tsx
const HasPasswordField = field("hasPassword")
	.schema(z.boolean())
	.render(({ register }) => (
		<label>
			<input type="checkbox" {...register()} />
			Has password?
		</label>
	));

const PasswordField = field("password")
	.dependsOn(HasPasswordField)
	.schema((formValues) => {
		if (formValues?.hasPassword) {
			return z.string().min(8, "Password must be at least 8 characters");
		}
		return z.string().optional();
	})
	.render(({ register, fieldState, watch }) => {
		if (!watch("hasPassword")) return null;
		return (
			<div>
				<input type="password" {...register()} />
				{fieldState.error && <span>{fieldState.error.message}</span>}
			</div>
		);
	});
```

<Callout type="warning">
	Include **all** fields (including those used in `dependsOn`) in the `fields` array of
	`zormyResolver` and, in wizards, in the correct step. Otherwise, typing and validation may become
	inconsistent.
</Callout>

## Nested fields (dot notation)

Dot keys create a nested structure in the form. The resolver automatically builds the nested Zod schema:

```tsx
const UserNameField = field("user.name")
	.schema(z.string().min(3))
	.render(({ register, fieldState }) => (
		<div>
			<label>Name</label>
			<input {...register()} />
			{fieldState.error && <span>{fieldState.error.message}</span>}
		</div>
	));

// defaultValues must reflect the structure:
// defaultValues: { user: { name: "" } }
```

Use the same pattern for `address.street`, `profile.bio`, etc.

## Hook `useField` (optional)

To access the form context with strong typing for a specific field, use `useField(key)` inside `<Form>` or `FormProvider`:

```tsx
import { useField } from "zormy";

function CustomNameInput() {
	const { register, fieldState } = useField("name");
	return (
		<div>
			<input {...register()} />
			{fieldState.error && <span>{fieldState.error.message}</span>}
		</div>
	);
}
```

In most cases, defining the field with `field("name").schema(...).render(...)` and using the generated component is sufficient.

## Summary

| Concept            | Usage                                                          |
| ------------------ | -------------------------------------------------------------- |
| Simple field       | `field("key").schema(z...).render(...)`                        |
| Reusable template  | `abstractField().schema(...).render(...)` + `.extend({ key })` |
| Conditional schema | `.dependsOn(OtherField).schema((formValues) => z...)`          |
| Nested structure   | Key `"a.b.c"` and `defaultValues: { a: { b: { c: "" } } }`     |

For Zod rules (min, max, email, refine, etc.) and error messages, see [Zod Integration](/docs/zod-integration). For the complete fields API, see [API Reference](/docs/api-reference).
